# openspec/config.yaml — Project configuration for verified-scala3 schema (v2)
#
# This file is injected into ALL artifact instructions so the AI always
# has project context. The stack below reflects what is DETECTED in this
# repository (see the capability-profile artifact of the active change);
# if this file disagrees with capability-profile.md, the profile wins.

schema: verified-scala3

# ── Project Context ──────────────────────────────────────────────────────
context: |
  STACK SOURCE OF TRUTH:
  - The authoritative stack description is capability-profile.md in the
    current change directory, generated by inspecting build.sbt,
    project/plugins.sbt, and source code. If this context section disagrees
    with capability-profile.md, the profile wins — update this file.
  - NEVER generate code or tests for a library that is not in the detected
    stack (e.g. do not generate otel4s code if otel4s is not a dependency,
    do not generate ScalaTest tests — this project uses munit).

  PROJECT: accordant4s — a Scala 3 model-based-testing library (port of
  Microsoft Accordant to the Typelevel stack). It is a LIBRARY, not a
  service: it exposes no API operations of its own.

  DETECTED STACK (verified against build.sbt / project/* on 2026-06-13):
  - Scala 3.8.4, sbt 1.12.11, JDK 26 (Homebrew OpenJDK)
  - Multi-module: `core` (accordant4s-core), `munit` (accordant4s-munit),
    `http4s` (accordant4s-http4s), `smithy4s` (accordant4s-smithy4s), each
    `dependsOn(core)`, aggregated by `root` (publish skipped)
  - scalacOptions: -Werror, -Wunused:all, -Wvalue-discard, -deprecation,
    -feature, -language:strictEquality, -language:adhocExtensions
  - Effect system: cats-effect 3.5.7; streaming: fs2-core 3.11.0 (core)
  - Refined types: Iron + iron-cats 3.0.2 (Compile, core) — deviates from this
    change's design.md "2.6.x" (latest available on Maven Central; new project,
    no migration cost). No iron-scalacheck/iron-hedgehog: refined-type
    generators are written by hand (raw value → smart constructor)
  - JSON/persistence: circe-core/generic/parser 0.14.13 (Compile, core)
  - HTTP: http4s-client/http4s-circe 0.23.30 (Compile, http4s module)
  - IDL: smithy4s-core 0.18.33 (Compile, smithy4s module) + sbt plugin
    smithy4s-sbt-codegen 0.18.33 (not yet enabled via enablePlugins —
    deferred to spec 7's TestBank.smithy fixture)
  - Test framework: munit 1.0.0 + munit-cats-effect 2.0.0 (NOT ScalaTest)
  - Property testing: Hedgehog (hedgehog-core Compile in core, hedgehog-munit
    0.13.1 Test) — integrated shrinking, NO Arbitrary typeclass, explicit
    `Range` sizing. Properties extend `hedgehog.munit.HedgehogSuite` (runs via
    munit's framework). NOT ScalaCheck/munit-scalacheck. `hedgehog.Gen` is a
    Compile dependency of core because `Operation.mock` exposes it publicly
  - sbt plugins: sbt-scalafix 0.14.0 (semanticdb enabled), sbt-scalafmt
    2.5.4, sbt-stryker4s 0.15.1, smithy4s-sbt-codegen 0.18.33
  - CONFIGURED (Ring 1): .scalafix.conf (DisableSyntax, RemoveUnused,
    OrganizeImports), .scalafmt.conf (scala3 dialect) — both verified via
    scalafixAll --check / scalafmtCheckAll+scalafmtSbtCheck
  - NOT AVAILABLE: WartRemover — no wartremover_3.8.4 build exists on Maven
    Central; omitted from project/plugins.sbt, Scalafix DisableSyntax covers
    the syntactic warts instead (see capability-profile.md)
  - READY, UNTESTED: stryker4s.conf present (Ring 5, threshold 80%,
    baseline mutate list — retarget per spec); Stainless (Ring 6) not
    installed, best-effort per spec; no TLA+/Apalache (Ring 7); no otel4s/
    Daut (Ring 9)

  CODING CONVENTIONS (correctness-first):
  - Invalid states should be unrepresentable: prefer narrow enums/ADTs and
    indexed ADTs/GADTs over broad enums validated downstream
  - All domain values crossing the library's public API use constrained
    opaque types (Iron) with smart constructors
  - Errors modeled explicitly as sealed enums, not exceptions
  - No side effects outside IO — no var, no null, no throw, no Future
  - NO silent fallbacks: no `case _` returning valid domain values for
    invalid input, no default mappings, no "unreachable" comments without
    a type-level proof
  - Pure domain packages must not import fs2/IO-effectful machinery
    (Ring 2 enforces layer rules from the design)
  - Given/When/Then format for requirements and scenarios

# ── Per-Artifact Rules ───────────────────────────────────────────────────
rules:
  proposal:
    - Always include a verification strategy with ring checkboxes
    - Ring 3 is mandatory for every code-changing spec; waivers only for docs/formatting/build-metadata/test-only changes, stated explicitly
    - Ring 8 (adversarial spec-compliance review) is mandatory for every code-changing spec
    - Include a correctness risk level (low/medium/high) with justification
    - Reference concept-inventory.md for existing types before proposing new ones
    - Include the typed-contract decision table (full/minimal/waiver per change kind)
    - If the change affects API operations or event sequences AND the telemetry stack is detected, Ring 9 MUST be checked

  capability-profile:
    - Detect by reading build files and source — never assume or copy example values
    - Record exact compile/test commands per module (used by typed contract and rings)
    - Record where typed-contract files must live to be genuinely compiled
    - Record whether stryker4s.conf uses a fixed mutate list (Ring 5 must retarget it)
    - Record which Scalafix/WartRemover rules are ACTIVE vs commented out

  concept-inventory:
    - Scan the ACTUAL project source tree, do not invent entries
    - Prefer the semantic (scala-cli/Scalameta) scanner over regex scanning
    - Include exact package paths (used for import statements in implementation)
    - Include exact refined-type constraints (used for type verification)
    - Run the consistency check (packages importable, constraints match source, generators compile) and record the result
    - For empty/new projects, create the tables with headers but no data rows

  specs:
    - Open every requirement with a normative SHALL/MUST statement before its Given clause (required by `openspec validate --strict`)
    - Use Given/When/Then format for ALL requirements; every Then must be observable
    - Every spec MUST have at least 2 Hedgehog properties in the Properties section
    - Properties must be INVARIANTS (forAll), not examples (specific values)
    - Every property must declare its generator strategy (constructive preferred over .ensure-filtering; no Arbitrary typeclass — generators are always explicit)
    - Include model-based properties where relevant (event-sourcing replay, messaging ingestion, DSL semantics, serialization round-trips)
    - Concepts Used table must reference exact entries from concept-inventory.md
    - Concepts Introduced table is a COMMITMENT — implementation must honor it
    - Include at least one error/edge case scenario per requirement
    - Include a Proof Obligations table mapping every requirement/invariant/constraint to an enforcement mechanism
    - Any "unreachable" claim needs a type-level proof obligation or explicit runtime check
    - Enum/GADT extensions must state how existing pattern matches behave for new variants

  spec-lint:
    - Fail fast — a FAIL verdict blocks design and implementation-order
    - Flag vague words (valid, fast, reasonable, correct, appropriate) without concrete definitions
    - Verify every proof obligation has a declared enforcement mechanism

  design:
    - Package structure must define project-specific layer rules from capability-profile.md (e.g. domain must not import the detected actor/protobuf/HTTP/messaging/DB libraries)
    - Effect boundaries section must classify every module as pure or effectful
    - Type strategy must place every invariant on the invalid-state-prevention hierarchy (impossible > smart constructor > validator > fallback > silent mapping) and justify the placement
    - Refined type strategy must justify every refined type AND every plain type
    - IDL model layout is REQUIRED if any spec has API operations
    - Compatibility story (Ring 4) is REQUIRED if any spec touches persisted or wire data
    - Verification map must assign rings to every module

  implementation-order:
    - Topological sort by concept dependency, not alphabetical
    - Simple specs before complex when no dependency constraint
    - Typed contract requirement recorded per spec (full/minimal/waiver) — mandatory for every code-changing spec
    - Record expected changed production files per spec (drives Ring 5 mutation targeting)
    - Every spec in the specs/ directory must appear in the sequence
